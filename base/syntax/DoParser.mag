// Keyword parser for "do" expressions.
class DoParser
    def parse(parser MagpieParser -> Expression)
        parser consume("do")
        ScopeExpression new(parser parseEndBlock())
    end
end

MagpieParser registerParseword("do", DoParser new())
