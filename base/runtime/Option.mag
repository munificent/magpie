union Option[T]
    Some T
    None
end

String[T] (option Option[T] -> String)
    match option
        case None       then "none"
        case Some value then "some " + value.String
    end
end
