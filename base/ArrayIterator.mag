Iterate'T (array []'T -> Array^'T) Array^ (array, -1)

MoveNext'T (iter Array^'T -> Bool)
    iter.Index <- + 1
    iter.Index < iter.Array.Size
end

Current'T (iter Array^'T -> T)
    def index <- iter.Index
    iter.Array.index 
end

struct Array^'T
            Array []'T
    mutable Index Int
end
