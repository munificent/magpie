// Top-level methods can be recursive.
def fib(n)
    if n < 2 then n else fib(n - 2) + fib(n - 1)
end

print(fib(10)) // expect: 55

// Can also be mutually recursive.
def foo(n)
    if n < 1 then "done" else bar(n - 1)
end

def bar(n)
    foo(n - 1)
end

print(bar(5)) // expect: done
