var channel = Channel new
async
    print("before send")
    channel send("sent value")
    print("after send")
end

print("before receive")
print(channel receive)
print("after receive")

// expect: before receive
// expect: before send
// expect: sent value
// expect: after receive
// expect: after send
// TODO(bob): Is "after send" what we want here? Should it stop after
// the main fiber completes?
