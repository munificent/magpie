// expected: true
// expected: false

Main (->)
    def a <- "foo"
    match a
        case "foo" then Print "true"
        case "bar" then Print "false"
        case _ then Print "else"
    end
    
    match "bar"
        case "foo" then Print "true"
        case "bar" then Print "false"
        case _ then Print "else"
    end
end
