// error line: 9
//### bob: ideally, the error would be on the line with the return,
// but at least we highlight the function

Main (->)
    def a <- Foo
end

Foo (-> Int)
    return 4
end
