// expected: if foo then bar else bang
// expected: if foo then bar else ()

Main (->)
    def a <- { if foo then bar else bang }
    Print a.String
    
    def b <- { if foo then bar }
    Print b.String
end
