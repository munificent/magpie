specify("The 'inherit' method on Class") with
    it should("add a class to the list of parent classes") with
        class Bar
        end

        class Foo
            inherit(Bar)
        end

        Foo parents count shouldEqual(1)
        Foo parents(0) shouldEqual(Bar)
    end
end
