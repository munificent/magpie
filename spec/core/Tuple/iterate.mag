specify("The 'iterate' method on Tuple") with
    it should("iterate through the fields") with
        var result = ""
        for field = ("one", "two", "three") do
            result = result ~ field
        end
        result shouldBe("onetwothree")
    end
end
