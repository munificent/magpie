// A simple class that is callable and tracks when it was called.
class TestCallable
    def call(arg)
        this arg = arg
        called = true
        returnValue
    end

    var arg = nothing
    var returnValue
    var called      Bool = false
end

def shared TestCallable new(returnValue)
    construct(returnValue: returnValue)
end

