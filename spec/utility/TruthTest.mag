// A simple class that implements truthiness and tracks when it was called.
class TruthTest
    this(value Bool) this value = value
    
    value Bool
    called = false
end

TruthTest defineGetter("true?", fn(-> Bool)
    called = true
    value
end)