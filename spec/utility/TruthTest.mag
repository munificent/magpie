// A simple class that implements truthiness and tracks when it was called.
class TruthTest
    get true? Bool =
        called = true
        value
    end

    var value  Bool
    var called Bool = false
end

def shared TruthTest new(value Bool)
    construct(value: value)
end

class NotTruable
    var true? = nothing
end