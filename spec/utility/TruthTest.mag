// A simple class that implements truthiness and tracks when it was called.
class TruthTest
    this(value Bool) =
        this value = value
        this called = false
    end
    
    get true? Bool =
        called = true
        value
    end

    var value  Bool
    var called Bool
end
