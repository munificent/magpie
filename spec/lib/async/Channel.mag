import spec.specify

import async

// TODO(bob): Uncomment when this is implemented.
/*
specifyMethod("(this is Channel) send(obj)") with
    it should("throw an error if the object is not async-safe") with
        val channel = Channel new()

        channel send("primitive")

        fn
            channel send(List new())
        end shouldThrow(UnsafeAsyncError)
    end
end
*/